syntax = "proto3";

package protoim;

option go_package = "./;protoim";

import "cmd.proto";

// 基本信息结构
message MessageBase {
  protoim.CMD cmd       = 1; // 指令值
  bytes data            = 2; // 原 pd 数据
}