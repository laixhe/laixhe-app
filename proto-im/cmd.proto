syntax = "proto3";

package protoim;

option go_package = "./;protoim";

// 指令类型
enum CMD {
  CError           = 0; // 错误
  CHeartBeat       = 1; // 心跳
  GetUserInfoReq   = 1000; // 获取用户信息
  GetUserInfoRes   = 1001; // 获取用户信息
}