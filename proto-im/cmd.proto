syntax = "proto3";

package protoim;

option go_package = "./;protoim";

// 指令类型
enum CMD {
  UNKNOWN        = 0; // 未知
  HEART_BEAT     = 1; // 心跳
  CHECKING_TOKEN = 2; // 检查令牌
  GET_USER_INFO  = 3; // 获取用户信息
}