syntax = "proto3";

package protoim;

option go_package = "./;protoim";

enum AppOs {
    OS_UNKNOWN = 0; // 未知
    WEB     = 1; // web
    IOS     = 2; // ios
    ANDROID = 3; // android
    PC      = 4; // pc
}